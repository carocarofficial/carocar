import Link from 'next/link';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const sp=await searchParams;return <main className="section"><div className="container authWrap"><div className="panel authCard"><div className="eyebrow">CAROCAR · INDIA</div><h1>Welcome back</h1><p className="muted">Sign in to save cars, compare vehicles and manage bookings.</p>{sp.error&&<div className="notice error">{sp.error}</div>}<form className="form" action="/api/auth/login" method="post"><label>Email<input className="input" name="email" type="email" placeholder="you@example.com" required/></label><label>Password<input className="input" name="password" type="password" placeholder="••••••••" required/></label><button className="btn primary">Log in</button></form><div className="demoBox"><b>Local demo account</b><br/>demo@carocar.in · carocar123</div><hr/><h2>Create account</h2><form className="form" action="/api/auth/register" method="post"><label>Name<input className="input" name="name" required/></label><label>City<input className="input" name="city" placeholder="Pune" required/></label><label>Email<input className="input" name="email" type="email" required/></label><label>Password<input className="input" name="password" type="password" minLength={6} required/></label><button className="btn ghost">Create account</button></form><p className="muted small">Local authentication uses an HTTP-only signed session cookie and server-side password hashing. Replace/configure the auth provider before production deployment.</p><Link href="/" className="muted">← Back to home</Link></div></div></main>}
