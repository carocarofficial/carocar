import { NextResponse } from 'next/server'; import { db } from '@/lib/db'; import { COOKIE, makeSession, verifyPassword } from '@/lib/auth'; import { z } from 'zod';
export async function POST(req:Request){try{const {email,password}=z.object({email:z.string().email(),password:z.string().min(6)}).parse(await req.formData().then(f=>({email:f.get('email'),password:f.get('password')}))); const user=await db.user.findUnique({where:{email:email.toLowerCase()}}); if(!user?.passwordHash||!verifyPassword(password,user.passwordHash)) return NextResponse.redirect(new URL('/login?error=Invalid%20email%20or%20password',req.url)); const res=NextResponse.redirect(new URL('/account',req.url)); res.cookies.set(COOKIE,makeSession(user.id),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',path:'/',maxAge:60*60*24*30}); return res;}catch{return NextResponse.redirect(new URL('/login?error=Invalid%20request',req.url));}}
