'use client';
import {useState} from 'react'; import Link from 'next/link';
const rentals=[{id:'demo-rental',brand:'Maruti Suzuki',model:'Baleno',mode:'SELF_DRIVE',rate:1800,location:'Pune'},{id:'driver-rental',brand:'Toyota',model:'Innova Crysta',mode:'WITH_DRIVER',rate:3800,location:'Mumbai'}];
export default function Rent(){const [date,setDate]=useState('');const [start,setStart]=useState('10:00');const [end,setEnd]=useState('18:00');const [msg,setMsg]=useState('');async function book(r:any){if(!date)return setMsg('Select a date first.');const startAt=`${date}T${start}:00`;const endAt=`${date}T${end}:00`;const res=await fetch('/api/rent/book',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rentalId:r.id,startAt,endAt,totalPrice:r.rate})});const d=await res.json();setMsg(res.ok?'Booking confirmed in the local demo database.':(d.error||'Login and use a seeded rental before booking.'));}return <main className="section"><div className="container"><div className="pageHead"><div><div className="eyebrow">RENT</div><h1>Rent a car</h1><p className="muted">Self-drive or with a driver, with server-side availability checks.</p></div><Link className="btn ghost" href="/account">My bookings</Link></div><div className="panel" style={{margin:'24px 0'}}><div className="grid2"><label>Date<input className="input" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>Start time<input className="input" type="time" value={start} onChange={e=>setStart(e.target.value)}/></label><label>End time<input className="input" type="time" value={end} onChange={e=>setEnd(e.target.value)}/></label></div>{msg&&<div className="notice">{msg}</div>}</div><div className="cards">{rentals.map(r=><article className="card" key={r.id}><div className="carImg">🚗</div><div className="cardbody"><span className="chip">{r.mode==='SELF_DRIVE'?'Self drive':'With driver'}</span><h3>{r.brand} {r.model}</h3><div className="price">₹{r.rate.toLocaleString('en-IN')}/day</div><p className="muted">{r.location} · Availability checked by the backend.</p><button className="btn primary" onClick={()=>book(r)}>Book vehicle</button></div></article>)}</div></div></main>}
