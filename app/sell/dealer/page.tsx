export default function DealerSell(){return <main className="section"><div className="container"><h1>Dealer workspace</h1><p className="muted">One verified dealer account can manage a complete store and inventory catalog.</p><div className="statgrid" style={{margin:'24px 0'}}>{[['Verification','Pending'],['Inventory','0 vehicles'],['Sold','0'],['Store','Not published']].map(x=><div className="stat" key={x[0]}><span className="muted">{x[0]}</span><strong>{x[1]}</strong></div>)}</div><div className="panel"><h2>Dealer profile & verification</h2><form className="form"><input className="input" placeholder="Store name"/><input className="input" placeholder="Business address"/><input className="input" placeholder="Contact phone"/><input className="input" type="file" multiple/><button className="btn primary">Submit verification</button></form></div><div className="panel" style={{marginTop:18}}><h2>Inventory actions</h2><p className="muted">Catalog upload, bulk inventory import, edit, reserve, sold status, warranty, inspection, financing, insurance, photos and videos.</p><button className="btn ghost">Upload catalog</button></div></div></main>}
